apiVersion: v1
kind: Service
metadata:
  name: violiert
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "violiert"
    port: 8080
  selector:
    app: violiert
