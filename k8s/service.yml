apiVersion: v1
kind: Service
metadata:
  name: badertion
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "badertion"
    port: 8080
  selector:
    app: badertion
