apiVersion: v1
kind: Service
metadata:
  name: heandr
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "heandr"
    port: 8080
  selector:
    app: heandr
