apiVersion: v1
kind: Service
metadata:
  name: uioervchza
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "uioervchza"
    port: 8080
  selector:
    app: uioervchza
